package com.maxcar.base.util.wechat;


public class MenuService {
    /**
     * 创建菜单
     * 系统启动 初始化就创建吧
     * */
    /*public static void createMenu(){
        int status = MenuUtil.createMenu(MenuUtil.getMenu());
		 //MenuUtil.deleteMenu(MenuUtil.getMenu());
		if(status==0){
			Logger.getRootLogger().info("菜单创建成功！");
		}else{
			Logger.getRootLogger().info("菜单创建失败！");
		}
	}*/

}
